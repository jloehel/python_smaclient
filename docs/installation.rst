.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install python_smaclient

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv python_smaclient
    $ pip install python_smaclient
